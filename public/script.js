const initMain = () => {
  const confirm = document.createElement('div');
  confirm.innerHTML = `<h1>Confirmation that script.js is imported and working</h1>`;
  document.body.appendChild(confirm);
}

initMain();